APP_HOST=http://app_host.io
APP_PORT=3000
SERVER_URL=server_url

PUBLIC_DIR=public_dir
REPO_DIR=repos

COOKIE_DOMAIN=cookie_domain
COOKIE_NAME=cookie_name

SESSION_DIR=sessions
SESSION_SECRET=session_secret

LOG_SILENT=true